#!/bin/bash

killall swaybarpp
if swaymsg -t get_outputs | grep transform | grep -q normal; then
    rot=270
    config=config-landscape
else
    rot=0
    config=config
fi

swaymsg "output DSI-1 transform $rot"
swaybarpp ~/.config/swaybarpp/$config.json &
